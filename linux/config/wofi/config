# wofi config - adapted from rofi

show=run
width=600
height=400
location=center
allow_markup=true
allow_images=false

# vim-like navigation. Modifier names are Shift/Ctrl/Alt (wofi-keys(7));
# Control_L is a key name and binds nothing.
key_up=Ctrl-k,Up
key_down=Ctrl-j,Down
