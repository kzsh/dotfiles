# The next line updates PATH for the Google Cloud SDK.
if [ -f "$HOME/vendor/google-cloud-sdk/path.bash.inc" ]; then . "$HOME/vendor/google-cloud-sdk/path.bash.inc"; fi

# The next line enables shell command completion for gcloud.
if [ -f "$HOME/vendor/google-cloud-sdk/completion.bash.inc" ]; then . "$HOME/vendor/google-cloud-sdk/completion.bash.inc"; fi
